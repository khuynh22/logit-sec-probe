version: '3.8'

services:
  # Research script - entropy analysis batch job
  entropy-analysis:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: logit-sec-probe-research
    volumes:
      # Mount output directory to get results on host
      - ./output:/app/output
      # Cache Hugging Face models to avoid re-downloading
      - hf-cache:/app/.cache/huggingface
    environment:
      - PYTHONUNBUFFERED=1
    # For GPU support, uncomment the following:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    profiles:
      - research  # Only run when explicitly specified

  # FastAPI backend - production API server
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: logit-sec-probe-api
    ports:
      - "8000:8000"
    volumes:
      # Cache Hugging Face models (shared with research service)
      - hf-cache:/app/.cache/huggingface
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - app-network
    # For GPU support, uncomment the following:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

  # Streamlit frontend - Security Observability Dashboard
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: logit-sec-probe-frontend
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  hf-cache:
